<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Assign Item</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
</head>
<body>
    <h2>Assign Item</h2>
    <p>
        <%= dataSupplier[0].name %>
    </p>
    <p>
        <%= dataSupplier[0].kota %>
    </p>
    <br>
    <h4>List Item</h4>
    <% dataConj.map(val => { %>
   
        <li><%= val.Item.name %> - <%= val.price %></li>

    <%})%>
    <br>
    <hr>
    <h4>Add Item</h4>
    <form action="/suppliers/<%= dataSupplier[0].id %>/additem" method="post">
        <select name="itemId">
            <% dataItem.map(val => { %>
               <option value="<%= val.id %>"><%= val.name %></option>
            <%})%>
        </select><br><br>
        <input type="text" placeholder="Ex: 900000000" name="price"><br><br>
        <button type="submit">Add item</button>
    </form>
</body>
</html>